<!DOCTYPE html>
<html>
<%- include('head') -%>

  <body>
    <%- include('header') -%>
    <% for (var i = 0; i < books.length; i++) { %>
      <div class="col justify-content-center">
          <div class="card justify-content-center" style="width: 8rem;margin: auto">
              <img class="card-img-top" src="<%= books[i].thumbnail %>">
              <div class="card-body">
                  <form action="/account/tradebook" method="POST">
                      <p class="card-title">
                          <%= books[i].title %>
                      </p>
                      <p class="card-text"><%= books[i].description %></p>
                      <input type="hidden" name="bookId" value="<%= books[i]._id %>">
                      <button href="#" class="btn btn-warning" type="submit">Trade</button>
                  </form>
              </div>
          </div>
      </div>
      <% } %>
  </body>

</html>